# Railway deployment configuration for RealtorPal Bot
# Deploy at: https://railway.app
# This runs the bot 24/7 for free on Railway's cloud

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python run_bot_simple.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
