# Railway Railpack / Nixpacks build configuration
# Installs Playwright's Chromium browser + all OS-level system library dependencies
# at BUILD time so they are baked into the image.

[phases.build]
cmds = ['playwright install --with-deps chromium']
