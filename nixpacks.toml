# Railway Railpack / Nixpacks build configuration
# Tells Railway to install Playwright's Chromium browser binary at BUILD time
# so it's baked into the image rather than downloaded on every container start.

[phases.build]
cmds = ['playwright install chromium --with-deps']
